---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["_ai-share/synth-1-full/src/ai/flows/detect-defects-flow.ts", "_ai-share/synth-1-full/src/app/api/brand/workshop2/qc/detect/route.ts"]
autonomous: true
requirements: ["1"]
must_haves:
  truths:
    - "System can analyze an image via Gemini 1.5 Flash and return structured defect coordinates and types"
    - "Defect detection API endpoint is accessible to the client"
  artifacts:
    - path: "_ai-share/synth-1-full/src/ai/flows/detect-defects-flow.ts"
      provides: "Genkit flow for defect detection"
      min_lines: 30
    - path: "_ai-share/synth-1-full/src/app/api/brand/workshop2/qc/detect/route.ts"
      provides: "HTTP endpoint for the Genkit flow"
      exports: ["POST"]
  key_links:
    - from: "_ai-share/synth-1-full/src/app/api/brand/workshop2/qc/detect/route.ts"
      to: "_ai-share/synth-1-full/src/ai/flows/detect-defects-flow.ts"
      via: "flow invocation"
      pattern: "detectDefectsFlow"
---

<objective>
Создать серверную логику для распознавания дефектов (AI Defect Detection) с использованием Genkit и Gemini 1.5 Flash.

Purpose: Снятие рутинной работы с инспектора ОТК путем предразметки дефектов (copilot mode) на фото/скетче.
Output: Genkit flow и API route, возвращающие JSON с массивом дефектов.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Из `_ai-share/synth-1-full/src/ai/genkit.ts`:
```typescript
import { genkit } from 'genkit';
import { googleAI } from '@genkit-ai/google-genai';
import { withTokenAudit } from './token-guard';
export const ai = genkit({
  plugins: [googleAI()],
  model: 'googleai/gemini-1.5-flash',
});
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создание Genkit Flow для детекции брака</name>
  <files>_ai-share/synth-1-full/src/ai/flows/detect-defects-flow.ts</files>
  <behavior>
    - Принимает base64 image или URL
    - Использует Gemini 1.5 Flash (multimodal)
    - Возвращает структуру: { defects: [{ type, description, boundingBox: { x, y, width, height } }] }
  </behavior>
  <action>
    Создать `detect-defects-flow.ts`. 
    Определить Zod схемы для входа (`{ imageBase64: z.string() }`) и выхода (`z.object({ defects: z.array(...) })`).
    Использовать `ai.defineFlow` из `src/ai/genkit.ts`. В промпте попросить модель проанализировать фото текстильного изделия, выявить дефекты (Пятно, Кривой шов, Разрыв, etc.) и вернуть координаты в процентах (0-100) от ширины и высоты изображения.
    Обернуть вызов в `withTokenAudit`.
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/ai/flows/detect-defects-flow.ts</automated>
  </verify>
  <done>Flow компилируется и экспортирует функцию detectDefectsFlow.</done>
</task>

<task type="auto">
  <name>Task 2: Создание HTTP API Endpoint</name>
  <files>_ai-share/synth-1-full/src/app/api/brand/workshop2/qc/detect/route.ts</files>
  <action>
    Создать POST обработчик в `/api/brand/workshop2/qc/detect/route.ts`.
    Эндпоинт должен принимать JSON body с `{ imageBase64: string }`.
    Вызывать `detectDefectsFlow(imageBase64)`.
    Обернуть в `observeApiRoute` (см. AGENTS.md).
    Возвращать `NextResponse.json` с результатом. Обрабатывать ошибки 500.
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/app/api/brand/workshop2/qc/detect/route.ts</automated>
  </verify>
  <done>API route корректно парсит body и вызывает Flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Прием больших base64 строк от клиента |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | API Route | mitigate | Ограничить размер payload в Next.js Route Config (limit size) и проверять в Zod. |
</threat_model>

<verification>
Убедиться, что endpoint `/api/brand/workshop2/qc/detect` доступен и компилируется.
</verification>

<success_criteria>
Серверная часть для AI Defect Detection готова и предоставляет строго типизированный контракт.
</success_criteria>

<output>
After completion, create `.planning/phases/6/06-01-SUMMARY.md`
</output>
