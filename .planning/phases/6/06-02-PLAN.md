---
phase: "06"
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: ["_ai-share/synth-1-full/src/components/brand/production/workshop2-qc-visual-inspection.tsx"]
autonomous: false
requirements: ["1", "2"]
must_haves:
  truths:
    - "User can click 'AI Scan' to auto-detect defects on the current image"
    - "AI detected defects are rendered as SVG bounding boxes or distinct AI pins"
    - "User can interact with pins and bounding boxes seamlessly"
  artifacts:
    - path: "_ai-share/synth-1-full/src/components/brand/production/workshop2-qc-visual-inspection.tsx"
      provides: "Visual Inspection UI with AI integration and bounding boxes"
  key_links:
    - from: "_ai-share/synth-1-full/src/components/brand/production/workshop2-qc-visual-inspection.tsx"
      to: "/api/brand/workshop2/qc/detect"
      via: "fetch within handleAIScan"
      pattern: "fetch\\(.*qc/detect.*\\)"
---

<objective>
Интегрировать AI Defect Detection в UI визуального инспектирования ОТК и добавить поддержку SVG-bounding boxes для точного указания области дефекта.

Purpose: Облегчить работу инспектора за счет предразметки фото и дать возможность выделять области (polygons/rectangles), а не только точки.
Output: Обновленный компонент Workshop2QcVisualInspection.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/6/06-RESEARCH.md
</context>

<interfaces>
From `_ai-share/synth-1-full/src/components/brand/production/workshop2-qc-visual-inspection.tsx`:
```typescript
export interface DefectPin {
  id: string;
  x: number;
  y: number;
  type: string;
  description?: string;
  // new fields:
  width?: number;
  height?: number;
  isAiSuggested?: boolean;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Добавление поддержки Bounding Boxes и SVG Overlay</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-qc-visual-inspection.tsx</files>
  <action>
    Обновить `DefectPin` интерфейс, добавив `width` и `height` (и опционально `isAiSuggested`).
    Изменить рендеринг пинов в `Workshop2QcVisualInspection`:
    Если у пина есть `width` и `height`, рендерить его как `<div className="absolute border-2 border-red-500 bg-red-500/20...">` (bounding box) с позиционированием через left, top, width, height в процентах. Если нет - рендерить текущий `LucideIcons.AlertTriangle` маркер.
    Попап (Popover) для редактирования дефекта должен работать для обоих видов маркеров.
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/components/brand/production/workshop2-qc-visual-inspection.tsx</automated>
  </verify>
  <done>Компонент рендерит пины с учетом ширины и высоты (bounding boxes).</done>
</task>

<task type="auto">
  <name>Task 2: Интеграция кнопки AI Scan</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-qc-visual-inspection.tsx</files>
  <action>
    Добавить кнопку "AI Анализ" (с иконкой LucideIcons.Sparkles) рядом с заголовком "Визуальная фиксация дефектов".
    При клике на кнопку:
    1. Перевести кнопку в состояние загрузки (spinner).
    2. Отправить `imageUrl` (возможно, переконвертировав в base64, если это локальный объект) на `/api/brand/workshop2/qc/detect`.
    3. Получив ответ `{ defects }`, добавить новые пины в стейт `pins` с флагом `isAiSuggested: true`.
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/components/brand/production/workshop2-qc-visual-inspection.tsx</automated>
  </verify>
  <done>Кнопка AI Scan делает запрос и обновляет стейт пинов.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>UI пиннинга дефектов и интеграция AI Scan</what-built>
  <how-to-verify>
    1. Открыть панель ОТК досье в интерфейсе приложения.
    2. Добавить фото в визуальную инспекцию.
    3. Кликнуть "AI Анализ", убедиться что появляются области дефектов (bounding boxes).
    4. Кликнуть по bounding box, чтобы открыть Popover и убедиться в работоспособности ручного редактирования.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/functional issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client | Клиент отображает данные от AI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | Visual Inspection UI | accept | Пользователь имеет право изменять/удалять предложенные AI пины. |
</threat_model>

<verification>
UI рендерится без ошибок, TS чекер проходит.
</verification>

<success_criteria>
Пользователь может автоматически получить области дефектов от AI и редактировать их в UI.
</success_criteria>

<output>
After completion, create `.planning/phases/6/06-02-SUMMARY.md`
</output>
