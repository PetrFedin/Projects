---
phase: "06"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified: ["_ai-share/synth-1-full/src/app/api/brand/workshop2/qc/supplier-scorecard/route.ts", "_ai-share/synth-1-full/src/components/brand/production/supplier-qc-scorecard.tsx", "_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-qc-panel.tsx"]
autonomous: true
requirements: ["3"]
must_haves:
  truths:
    - "User can view a scorecard widget for a supplier"
    - "Scorecard displays AQL success rates and defect statistics"
    - "Widget is integrated into the QC Panel or Supplier View"
  artifacts:
    - path: "_ai-share/synth-1-full/src/components/brand/production/supplier-qc-scorecard.tsx"
      provides: "Recharts-based scorecard widget"
    - path: "_ai-share/synth-1-full/src/app/api/brand/workshop2/qc/supplier-scorecard/route.ts"
      provides: "API endpoint for fetching scorecard data"
  key_links:
    - from: "_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-qc-panel.tsx"
      to: "_ai-share/synth-1-full/src/components/brand/production/supplier-qc-scorecard.tsx"
      via: "import and render"
      pattern: "<SupplierQcScorecard"
---

<objective>
Создать `Supplier Scorecard` — авто-рейтинг фабрик на основе статистики AQL, и визуализировать его с помощью `recharts`.

Purpose: Показать агрегированную статистику качества (Pass Rate, Rework Rate) фабрики в интерфейсе.
Output: API endpoint для агрегации данных и UI-виджет SupplierQcScorecard.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/6/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API Route Supplier Scorecard</name>
  <files>_ai-share/synth-1-full/src/app/api/brand/workshop2/qc/supplier-scorecard/route.ts</files>
  <action>
    Создать GET эндпоинт `/api/brand/workshop2/qc/supplier-scorecard/route.ts`.
    Эндпоинт принимает `supplierId` (query param) и возвращает агрегированную статистику:
    `{ totalBatches: number, passed: number, failed: number, rework: number, passRate: number, defectTypes: { name: string, value: number }[] }`.
    Реализовать моковые данные (до подключения реальной read-model базы), чтобы UI мог корректно рендериться. Обернуть в `observeApiRoute`.
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/app/api/brand/workshop2/qc/supplier-scorecard/route.ts</automated>
  </verify>
  <done>Эндпоинт возвращает типизированную статистику поставщика.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создание виджета SupplierQcScorecard</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/supplier-qc-scorecard.tsx</files>
  <behavior>
    - Виджет принимает `supplierId` как prop
    - Выполняет fetch (через SWR или useEffect) к эндпоинту `supplier-scorecard`
    - Рендерит кольцевой график (PieChart из recharts) со статусами партий
  </behavior>
  <action>
    Создать React компонент `SupplierQcScorecard`. Использовать `WidgetContainer` или `ChartCard` (см. `AGENTS.md`).
    Отображать метрику Pass Rate крупным шрифтом (MetricCard стиль).
    Использовать `recharts` (PieChart) для визуализации пропорции (passed, rework, failed).
    Использовать `recharts` (BarChart) для отображения `defectTypes` (самые частые дефекты).
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/components/brand/production/supplier-qc-scorecard.tsx</automated>
  </verify>
  <done>Компонент рендерит графики без ошибок TS.</done>
</task>

<task type="auto">
  <name>Task 3: Интеграция Scorecard в QC Panel</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-qc-panel.tsx</files>
  <action>
    Импортировать `SupplierQcScorecard` в `Workshop2ArticleQcPanel`.
    Добавить новую секцию "Рейтинг производителя" внизу панели или во вкладке.
    Передать `dossier?.contractorId` (если есть, иначе заглушку "supplier-1") в виджет.
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-qc-panel.tsx</automated>
  </verify>
  <done>Scorecard виджет отображается в интерфейсе ОТК панели.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB -> Read Model | Агрегация данных по поставщику |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | API Route | mitigate | Эндпоинт должен проверять доступ (RBAC) текущего пользователя к данным поставщика (опционально, если B2B_V1_API_ENFORCE_ROLES включен). |
</threat_model>

<verification>
Виджет отображается, данные запрашиваются, графики recharts рендерятся.
</verification>

<success_criteria>
Пользователь видит наглядную статистику фабрики по качеству.
</success_criteria>

<output>
After completion, create `.planning/phases/6/06-03-SUMMARY.md`
</output>
