---
phase: 07-intake
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - _ai-share/synth-1-full/src/components/brand/production/intake/use-rfid-scanner.ts
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-stock-panel.tsx
  - _ai-share/synth-1-full/src/app/api/b2b/intake/reconcile/route.ts
autonomous: true
requirements: [REQ-7.3]

must_haves:
  truths:
    - "RFID scanner hook aggregates EPC scans and debounces them"
    - "Stock panel UI displays scanned RFID tags and reconciliation status"
    - "Reconciliation API matches scanned tags against PO"
  artifacts:
    - path: "_ai-share/synth-1-full/src/components/brand/production/intake/use-rfid-scanner.ts"
      provides: "RFID scanner hook"
    - path: "_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-stock-panel.tsx"
      provides: "Updated Stock Panel with RFID UI"
    - path: "_ai-share/synth-1-full/src/app/api/b2b/intake/reconcile/route.ts"
      provides: "PO Reconciliation API"
  key_links:
    - from: "_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-stock-panel.tsx"
      to: "_ai-share/synth-1-full/src/components/brand/production/intake/use-rfid-scanner.ts"
      via: "UI uses scanner hook"
      pattern: "useRfidScanner"
    - from: "_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-stock-panel.tsx"
      to: "_ai-share/synth-1-full/src/app/api/b2b/intake/reconcile/route.ts"
      via: "UI sends batched EPCs to reconcile API"
      pattern: "fetch.*api/b2b/intake/reconcile"
---

<objective>
Implement RFID Auto-Intake to aggregate mass scans and reconcile them with Purchase Orders.

Purpose: Accelerate physical intake using RFID and WebHID/Keyboard Wedge emulation, avoiding over-scanning and API overload.
Output: Client-side scanner hook, updated Stock Panel UI, and reconciliation API.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/7/07-RESEARCH.md
@.planning/phases/07-intake/07-01-PLAN.md
@.planning/phases/07-intake/07-02-PLAN.md
</context>

<interfaces>
From _ai-share/synth-1-full/src/lib/b2b/intake/types.ts:
(Defined in Plan 01)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement RFID Scanner Hook</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/intake/use-rfid-scanner.ts</files>
  <action>
    Implement `useRfidScanner` hook that listens for keyboard events (emulating an RFID wedge scanner).
    It must aggregate scanned EPCs into a `Set` to deduplicate and debounce/throttle the sending of batches (e.g., every 500ms) to prevent API overload.
    Include a programmatic way to simulate scans for development purposes.
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/components/brand/production/intake/use-rfid-scanner.ts</automated>
  </verify>
  <done>Hook is implemented, handles debouncing, and type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PO Reconciliation API</name>
  <files>_ai-share/synth-1-full/src/app/api/b2b/intake/reconcile/route.ts, _ai-share/synth-1-full/src/lib/b2b/intake/rfid-reconciliation.test.ts</files>
  <action>
    Create a POST endpoint that accepts an array of EPCs and a PO reference.
    Implement the logic to match the EPCs against the expected items in the PO.
    Return the reconciliation result (matched, missing, unexpected).
    Update `rfid-reconciliation.test.ts` to test this core logic and make it pass.
  </action>
  <verify>
    <automated>npx jest _ai-share/synth-1-full/src/lib/b2b/intake/rfid-reconciliation.test.ts</automated>
  </verify>
  <done>API endpoint is created and reconciliation logic passes tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update Stock Panel UI</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-stock-panel.tsx</files>
  <action>
    Integrate `useRfidScanner` into `Workshop2ArticleStockPanel`.
    Add UI elements to display the current scanning session (scanned EPC count, recent scans).
    Add a button to manually trigger the mock scanner for testing.
    On batch scan, call the `/api/b2b/intake/reconcile` endpoint and display the reconciliation status (matched vs unexpected).
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-stock-panel.tsx</automated>
  </verify>
  <done>UI is updated with RFID scanning capabilities and type-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | RFID EPC batch submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-1 | Spoofing | Reconcile API | mitigate | Validate EPC format and match against expected PO database. |
| T-07-04-2 | Denial of Service | Reconcile API | mitigate | Client-side debouncing and batching of EPCs. |
</threat_model>

<verification>
Reconciliation tests pass, and all modified files type-check without errors.
</verification>

<success_criteria>
RFID scanning is emulated in the browser, batches are sent to the API, and reconciled against POs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-intake/07-04-SUMMARY.md`
</output>
