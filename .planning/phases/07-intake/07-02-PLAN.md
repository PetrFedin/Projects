---
phase: 07-intake
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - _ai-share/synth-1-full/src/lib/b2b/allocation/allocation-engine.ts
  - _ai-share/synth-1-full/src/app/api/b2b/intake/allocate/route.ts
autonomous: true
requirements: [REQ-7.1]

must_haves:
  truths:
    - "Smart Allocation distributes stock according to B2B priority strategy"
    - "API endpoint exists to trigger allocation for an intake batch"
  artifacts:
    - path: "_ai-share/synth-1-full/src/lib/b2b/allocation/allocation-engine.ts"
      provides: "Allocation Engine and B2BPriorityStrategy"
    - path: "_ai-share/synth-1-full/src/app/api/b2b/intake/allocate/route.ts"
      provides: "Allocation API Endpoint"
  key_links:
    - from: "_ai-share/synth-1-full/src/app/api/b2b/intake/allocate/route.ts"
      to: "_ai-share/synth-1-full/src/lib/b2b/allocation/allocation-engine.ts"
      via: "API calls allocation engine"
      pattern: "new B2BPriorityStrategy"
---

<objective>
Implement the Smart Allocation Engine using the Strategy pattern to distribute intake batches across B2B, retail, and e-commerce channels.

Purpose: Automate inventory distribution based on demand, prioritizing B2B backorders.
Output: Working allocation engine and its API endpoint.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/7/07-RESEARCH.md
@.planning/phases/07-intake/07-01-PLAN.md
</context>

<interfaces>
From _ai-share/synth-1-full/src/lib/b2b/allocation/types.ts:
(Defined in Plan 01)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Allocation Engine</name>
  <files>_ai-share/synth-1-full/src/lib/b2b/allocation/allocation-engine.ts, _ai-share/synth-1-full/src/lib/b2b/allocation/allocation-engine.test.ts</files>
  <behavior>
    - Test 1: B2BPriorityStrategy allocates to B2B backorders first.
    - Test 2: Remaining stock is split between retail and e-com.
  </behavior>
  <action>
    Implement `B2BPriorityStrategy` conforming to `AllocationStrategy`.
    Ensure the logic correctly deducts from the intake batch and assigns quantities to the respective channels. Make tests pass.
  </action>
  <verify>
    <automated>npx jest _ai-share/synth-1-full/src/lib/b2b/allocation/allocation-engine.test.ts</automated>
  </verify>
  <done>Allocation engine passes all tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Allocation API Endpoint</name>
  <files>_ai-share/synth-1-full/src/app/api/b2b/intake/allocate/route.ts</files>
  <action>
    Create a POST endpoint that accepts an `IntakeBatch` and `DemandProfile`, validates the payload with Zod, invokes the `B2BPriorityStrategy`, and returns the `AllocationPlan`.
    Use a mock transaction or note for pessimistic locking (`SELECT ... FOR UPDATE`) as per research to prevent race conditions.
  </action>
  <verify>
    <automated>npx tsc --noEmit _ai-share/synth-1-full/src/app/api/b2b/intake/allocate/route.ts</automated>
  </verify>
  <done>API endpoint handles POST requests and returns allocation plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Intake batch data submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-1 | Spoofing | Allocate API | mitigate | Require warehouse manager role/auth. |
| T-07-02-2 | Tampering | Allocate API | mitigate | Validate payload with Zod. |
| T-07-02-3 | Information Disclosure | Allocate API | mitigate | Ensure race conditions are handled via transactions. |
</threat_model>

<verification>
Allocation engine tests pass and API endpoint type-checks.
</verification>

<success_criteria>
Smart allocation logic is functional and exposed via API.
</success_criteria>

<output>
After completion, create `.planning/phases/07-intake/07-02-SUMMARY.md`
</output>
