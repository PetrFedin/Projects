---
phase: 13-collaborative-workspace
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - src/lib/notifications/triggers.ts
  - src/components/notifications/NotificationPreferences.tsx
  - src/app/api/notifications/trigger-event/route.ts
autonomous: true
requirements: [3]
must_haves:
  truths:
    - "System supports new event triggers: PO status change, raw material delay, QC results"
    - "User can configure notification preferences for these events"
    - "System can trigger these events via API"
  artifacts:
    - path: "src/lib/notifications/triggers.ts"
      provides: "Updated trigger definitions"
    - path: "src/components/notifications/NotificationPreferences.tsx"
      provides: "UI for managing notification settings"
    - path: "src/app/api/notifications/trigger-event/route.ts"
      provides: "Endpoint to trigger mock events"
  key_links:
    - from: "src/app/api/notifications/trigger-event/route.ts"
      to: "src/lib/notifications/triggers.ts"
      via: "validation"
      pattern: "TriggerType"
---

<objective>
Expand the Event Notification Hub to support new collaborative events (PO status changes, delays, QC results) and allow users to configure their preferences.

Purpose: Keep all stakeholders informed of critical supply chain events in real-time.
Output: Updated triggers, a preferences UI, and a test endpoint for triggering events.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Notification Triggers</name>
  <files>src/lib/notifications/triggers.ts</files>
  <action>Add `po_status_change`, `raw_material_delay`, and `qc_result` to `TriggerType` and `DEFAULT_TRIGGERS`. Provide appropriate labels and descriptions for each.</action>
  <verify>
    <automated>npm run typecheck:order-subset</automated>
  </verify>
  <done>New triggers are available in the type system and default configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Build Notification Preferences UI</name>
  <files>src/components/notifications/NotificationPreferences.tsx</files>
  <action>Create a component that lists all available triggers from `DEFAULT_TRIGGERS` and provides toggle switches for Email, Push, and In-App notifications for each. Use a mock save function with a toast notification.</action>
  <verify>
    <automated>npm run lint:order-subset</automated>
  </verify>
  <done>Users can toggle preferences for the new event types.</done>
</task>

<task type="auto">
  <name>Task 3: Create Event Trigger Endpoint</name>
  <files>src/app/api/notifications/trigger-event/route.ts</files>
  <action>Create a POST endpoint that accepts a `TriggerType` and payload. It should validate the trigger type and use the existing notification infrastructure (or mock it) to dispatch the event. This will be used for testing the hub.</action>
  <verify>
    <automated>curl -X POST -H "Content-Type: application/json" -d '{"type":"po_status_change","title":"PO Updated","body":"Status changed to In Production"}' http://localhost:3000/api/notifications/trigger-event</automated>
  </verify>
  <done>Endpoint successfully receives and processes event triggers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Notification System | Untrusted event triggers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Spoofing | /api/notifications/trigger-event | mitigate | Validate `type` against allowed `TriggerType` values before processing |
</threat_model>

<verification>
- New triggers are recognized by the system.
- Preferences UI renders correctly with all triggers.
- Trigger endpoint accepts valid requests and rejects invalid ones.
</verification>

<success_criteria>
- The notification hub supports the new collaborative events required for Phase 13.
- Users have control over how they receive these notifications.
</success_criteria>

<output>
After completion, create `.planning/phases/13-collaborative-workspace/13-03-SUMMARY.md`
</output>
