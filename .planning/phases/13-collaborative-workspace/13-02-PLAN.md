---
phase: 13-collaborative-workspace
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/vendor/layout.tsx
  - src/app/vendor/page.tsx
  - src/app/vendor/tech-pack/[articleId]/page.tsx
  - src/components/vendor/VendorDocumentUpload.tsx
autonomous: true
requirements: [2]
must_haves:
  truths:
    - "Vendor can access a dedicated portal layout"
    - "Vendor can view a read-only tech pack"
    - "Vendor can upload certificates or documents"
  artifacts:
    - path: "src/app/vendor/layout.tsx"
      provides: "Vendor portal shell"
    - path: "src/app/vendor/tech-pack/[articleId]/page.tsx"
      provides: "Read-only tech pack view for vendors"
    - path: "src/components/vendor/VendorDocumentUpload.tsx"
      provides: "Document upload component"
  key_links:
    - from: "src/app/vendor/layout.tsx"
      to: "src/app/vendor/page.tsx"
      via: "Next.js app router"
      pattern: "children"
---

<objective>
Build the External Vendor Portal to provide limited web access for factories and suppliers to view Tech Packs and upload certificates.

Purpose: Securely share necessary production details with external partners without exposing brand economics.
Output: A new `/vendor` route group with a dashboard, read-only tech pack view, and document upload component.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vendor Portal Layout and Dashboard</name>
  <files>src/app/vendor/layout.tsx, src/app/vendor/page.tsx</files>
  <action>Create a new layout for the `/vendor` route that includes a simple navigation bar (Dashboard, Tech Packs, Documents). Create a dashboard page that lists assigned tasks or recent tech packs (mock data). Ensure it uses the design system primitives.</action>
  <verify>
    <automated>npm run lint:order-subset</automated>
  </verify>
  <done>Vendor layout and dashboard render without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build Read-Only Tech Pack View</name>
  <files>src/app/vendor/tech-pack/[articleId]/page.tsx</files>
  <action>Create a page that displays a read-only version of a Tech Pack. It should show basic article details, BOM (without pricing), and measurement charts. Use mock data or fetch from existing v1 APIs if available.</action>
  <verify>
    <automated>npm run typecheck:order-subset</automated>
  </verify>
  <done>Tech pack page renders article details without exposing sensitive financial data.</done>
</task>

<task type="auto">
  <name>Task 3: Create Document Upload Component</name>
  <files>src/components/vendor/VendorDocumentUpload.tsx, src/app/vendor/page.tsx</files>
  <action>Create a `VendorDocumentUpload` component allowing vendors to upload certificates (e.g., ISO, OEKO-TEX). Use a mock upload function with a simulated delay and success toast. Integrate this component into the vendor dashboard.</action>
  <verify>
    <automated>npm run lint:order-subset</automated>
  </verify>
  <done>Vendors can simulate uploading a document and see a success state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendor -> Brand Data | Vendor portal must not expose brand pricing or internal notes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Information Disclosure | /vendor/tech-pack | mitigate | Ensure tech pack view explicitly omits pricing and cost fields from the UI and API calls |
</threat_model>

<verification>
- `/vendor` route is accessible.
- Tech pack view does not contain pricing information.
- Document upload simulates successfully.
</verification>

<success_criteria>
- Vendor portal provides a distinct, limited view compared to the brand portal.
- All components use the established design system.
</success_criteria>

<output>
After completion, create `.planning/phases/13-collaborative-workspace/13-02-SUMMARY.md`
</output>
