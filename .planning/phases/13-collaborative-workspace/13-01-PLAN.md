---
phase: 13-collaborative-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/brand/messages/ContextualChatThread.tsx
  - src/app/api/messages/contextual/route.ts
  - src/components/brand/production/Workshop2VendorBiddingPanel.tsx
autonomous: true
requirements: [1]
must_haves:
  truths:
    - "User can view a chat thread tied to a specific context (e.g., a bid or BOM row)"
    - "User can add a new message to the contextual thread"
    - "Chat messages persist for that specific context"
  artifacts:
    - path: "src/components/brand/messages/ContextualChatThread.tsx"
      provides: "UI for contextual chat threads"
    - path: "src/app/api/messages/contextual/route.ts"
      provides: "API for CRUD operations on contextual messages"
  key_links:
    - from: "src/components/brand/messages/ContextualChatThread.tsx"
      to: "/api/messages/contextual"
      via: "fetch"
      pattern: "fetch.*api/messages/contextual"
---

<objective>
Implement Contextual Threading to allow users to attach chat threads to specific contexts like a BOM row, a 3D avatar point, or a vendor bid.

Purpose: Enable focused communication without losing context.
Output: A reusable `ContextualChatThread` component and its backing API, integrated into the vendor bidding panel as a proof of concept.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Contextual Messages API</name>
  <files>src/app/api/messages/contextual/route.ts</files>
  <action>Create a GET and POST endpoint for contextual messages. It should accept `contextType` (e.g., 'bid', 'bom_row') and `contextId`. Use an in-memory store (like `getOrCreateGlobalRuntime`) for mock persistence.</action>
  <verify>
    <automated>curl -X POST -H "Content-Type: application/json" -d '{"contextType":"bid","contextId":"123","message":"Test"}' http://localhost:3000/api/messages/contextual</automated>
  </verify>
  <done>API successfully stores and retrieves messages by contextType and contextId.</done>
</task>

<task type="auto">
  <name>Task 2: Build ContextualChatThread Component</name>
  <files>src/components/brand/messages/ContextualChatThread.tsx</files>
  <action>Create a React component that takes `contextType` and `contextId` as props. It should fetch messages from the new API, display them in a scrollable list, and provide an input to send new messages. Use Tailwind and Lucide icons for styling.</action>
  <verify>
    <automated>npm run lint:order-subset</automated>
  </verify>
  <done>Component renders correctly and handles message submission.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into Vendor Bidding Panel</name>
  <files>src/components/brand/production/Workshop2VendorBiddingPanel.tsx</files>
  <action>Update `Workshop2VendorBiddingPanel` to replace the "Чат с представителем фабрики недоступен" toast with rendering the `ContextualChatThread` component for a specific bid when the "Обсудить" button is clicked (e.g., in a Vaul drawer or Dialog).</action>
  <verify>
    <automated>npm run typecheck:order-subset</automated>
  </verify>
  <done>Users can open a contextual chat for a specific vendor bid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted chat messages submitted to the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | /api/messages/contextual | mitigate | Sanitize and validate incoming message payloads to prevent XSS |
</threat_model>

<verification>
- Contextual messages can be created and viewed.
- Messages are isolated by contextType and contextId.
</verification>

<success_criteria>
- `ContextualChatThread` component is reusable across different contexts.
- Vendor bidding panel successfully demonstrates contextual threading.
</success_criteria>

<output>
After completion, create `.planning/phases/13-collaborative-workspace/13-01-SUMMARY.md`
</output>
