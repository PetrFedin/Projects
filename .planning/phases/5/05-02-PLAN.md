---
phase: 05-production
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - _ai-share/synth-1-full/src/lib/production/article-workspace/types.ts
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-operation-media.tsx
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-release-panel.tsx
autonomous: true
requirements:
  - Видео-инструкции для швей
must_haves:
  truths:
    - User can attach a media URL to an operation.
    - User can watch an auto-playing loop video instruction for complex nodes.
  artifacts:
    - path: _ai-share/synth-1-full/src/components/brand/production/workshop2-operation-media.tsx
      provides: Video loop component
      min_lines: 10
    - path: _ai-share/synth-1-full/src/lib/production/article-workspace/types.ts
      provides: Types containing mediaUrl
  key_links:
    - from: _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-release-panel.tsx
      to: _ai-share/synth-1-full/src/components/brand/production/workshop2-operation-media.tsx
      via: Rendered inside the operations table or row expansion
---

<objective>
Integrate short video-instructions (MP4/GIF) for complex nodes in production tracking.

Purpose: Help operators quickly understand complex sewing nodes via looping visual instructions.
Output: Native video player component and schema updates for operations.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/5/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Types</name>
  <files>_ai-share/synth-1-full/src/lib/production/article-workspace/types.ts</files>
  <action>Update `ProductionOperation` interface to include `mediaUrl?: string`. This will store the link to the MP4/GIF file.</action>
  <verify>
    <automated>grep "mediaUrl" _ai-share/synth-1-full/src/lib/production/article-workspace/types.ts</automated>
  </verify>
  <done>ProductionOperation includes mediaUrl</done>
</task>

<task type="auto">
  <name>Task 2: Create Operation Media Component</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-operation-media.tsx</files>
  <action>Create a `Workshop2OperationMedia` component displaying an HTML5 `<video>` tag with attributes `autoPlay loop muted playsInline` to show the instruction without blocking user interaction.</action>
  <verify>
    <automated>grep -i "video" _ai-share/synth-1-full/src/components/brand/production/workshop2-operation-media.tsx</automated>
  </verify>
  <done>Component returns a native muted looping video element</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Media in Release Panel</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-release-panel.tsx</files>
  <action>Update the operations table in `Workshop2ArticleReleasePanel` to allow users to input a `mediaUrl` (e.g. via a dialog or inline input) and display the `Workshop2OperationMedia` component if a URL exists.</action>
  <verify>
    <automated>grep "mediaUrl" _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-release-panel.tsx</automated>
  </verify>
  <done>Release panel correctly mutates and renders media URLs</done>
</task>

</tasks>

<success_criteria>
Operations support attaching and displaying MP4/GIFs through native HTML5 video tags that play automatically and silently.
</success_criteria>

<output>
After completion, create `.planning/phases/5/05-02-SUMMARY.md`
</output>
