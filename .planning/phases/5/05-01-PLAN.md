---
phase: 05-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _ai-share/synth-1-full/package.json
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-routing-sheet-print.tsx
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-release-panel.tsx
autonomous: true
requirements:
  - MES Barcoding (WIP)
must_haves:
  truths:
    - User can generate and view a print-ready routing sheet with a QR code.
  artifacts:
    - path: _ai-share/synth-1-full/src/components/brand/production/workshop2-routing-sheet-print.tsx
      provides: Print layout with qrcode.react
      min_lines: 20
  key_links:
    - from: _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-release-panel.tsx
      to: _ai-share/synth-1-full/src/components/brand/production/workshop2-routing-sheet-print.tsx
      via: Print button renders the layout
---

<objective>
Generate MES routing sheets with QR codes for tablet tracking.

Purpose: Allow physical tracking of bundles on the shop floor by scanning QR routing sheets.
Output: Print layout component and integration in the release panel.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/5/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add qrcode.react dependency</name>
  <files>_ai-share/synth-1-full/package.json</files>
  <action>Add `qrcode.react` to package.json dependencies and install it. Use exact or compatible version (e.g. `^4.1.0`).</action>
  <verify>
    <automated>cat _ai-share/synth-1-full/package.json | grep qrcode.react</automated>
  </verify>
  <done>Dependency is installed</done>
</task>

<task type="auto">
  <name>Task 2: Create Routing Sheet Component</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-routing-sheet-print.tsx</files>
  <action>Create a component `Workshop2RoutingSheetPrint` that takes `bundleId` (or `articleId`) and `operations` as props. It must render a print-friendly layout featuring a prominent QR code (using `QRCodeSVG` from `qrcode.react`) embedding `synth://bundle/${bundleId}` and a list of operations.</action>
  <verify>
    <automated>ls _ai-share/synth-1-full/src/components/brand/production/workshop2-routing-sheet-print.tsx</automated>
  </verify>
  <done>Component correctly renders a QRCodeSVG</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into Release Panel</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-release-panel.tsx</files>
  <action>Add a "Print Route Sheet" button in `Workshop2ArticleReleasePanel`. When clicked, it should either open a dialog containing the `Workshop2RoutingSheetPrint` component or use window.print() on a hidden printable area containing it.</action>
  <verify>
    <automated>grep -q "Workshop2RoutingSheetPrint" _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-release-panel.tsx</automated>
  </verify>
  <done>Button exists and triggers the print layout</done>
</task>

</tasks>

<success_criteria>
QR code generation works without server roundtrips, layout is print-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/5/05-01-SUMMARY.md`
</output>
