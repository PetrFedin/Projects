---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _ai-share/synth-1-full/src/lib/types/material-engineering.ts
  - _ai-share/synth-1-full/src/app/api/brand/workshop2/materials/lab-dips/route.ts
  - _ai-share/synth-1-full/src/components/brand/production/Workshop2MaterialLabDipsPanel.tsx
autonomous: true
requirements: [1]
must_haves:
  truths:
    - User can see a list of lab dips and strike-offs for a material.
    - User can approve or reject a lab dip/strike-off from the Tech Pack.
    - Changes to lab dip statuses are saved and displayed.
  artifacts:
    - path: _ai-share/synth-1-full/src/lib/types/material-engineering.ts
      provides: Types for Lab Dips and Strike-offs
    - path: _ai-share/synth-1-full/src/app/api/brand/workshop2/materials/lab-dips/route.ts
      provides: API endpoint for CRUD on Lab Dips
    - path: _ai-share/synth-1-full/src/components/brand/production/Workshop2MaterialLabDipsPanel.tsx
      provides: UI component for Lab Dips tracking
  key_links:
    - from: _ai-share/synth-1-full/src/components/brand/production/Workshop2MaterialLabDipsPanel.tsx
      to: _ai-share/synth-1-full/src/app/api/brand/workshop2/materials/lab-dips/route.ts
      via: fetch
      pattern: fetch.*api/brand/workshop2/materials/lab-dips
---

<objective>
Implement tracking and approval for Lab Dips and Strike-offs directly in the Tech Pack.

Purpose: Allow brands and suppliers to manage the lifecycle of custom fabric dyeing and printing before production.
Output: A new UI panel for the material hub and corresponding BFF endpoints to track and approve these artifacts.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Material Engineering types</name>
  <files>_ai-share/synth-1-full/src/lib/types/material-engineering.ts</files>
  <action>Create TypeScript interfaces for Lab Dips and Strike-offs. Include fields like `id`, `materialId`, `type` (lab-dip, strike-off), `status` (pending, approved, rejected), `submittedAt`, `notes`, and `imageUrl`. Export these types.</action>
  <verify>File exists and has no TS errors.</verify>
  <done>Types are defined and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Build API endpoint for Lab Dips</name>
  <files>_ai-share/synth-1-full/src/app/api/brand/workshop2/materials/lab-dips/route.ts</files>
  <action>Create a Next.js API route that handles GET and POST requests for lab dips. For GET, return mock data. For POST, accept status updates (approve/reject) and return the updated object.</action>
  <verify>Endpoint compiles and returns valid JSON for GET requests.</verify>
  <done>Endpoint supports GET/POST and returns appropriate DTOs.</done>
</task>

<task type="auto">
  <name>Task 3: Build Lab Dips UI Panel</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/Workshop2MaterialLabDipsPanel.tsx</files>
  <action>Create a React component using the design system (Card, Button, Badge) to display a list of lab dips. Add 'Approve' and 'Reject' buttons that trigger the API endpoint and optimistically update the UI. Display current status with appropriate color badges.</action>
  <verify>Component compiles and can be imported without errors.</verify>
  <done>UI renders the list of lab dips and handles approval actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Brand users sending approval statuses to the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | Lab Dips API | mitigate | Validate status enum (pending, approved, rejected) using Zod before updating |
</threat_model>

<verification>
Check that the API endpoint responds to GET. Check that the UI panel can be mounted without crashing.
</verification>

<success_criteria>
Types, API, and UI for tracking lab dips and strike-offs are implemented and connected.
</success_criteria>

<output>
After completion, create `.planning/phases/12/{phase}-{plan}-SUMMARY.md`
</output>