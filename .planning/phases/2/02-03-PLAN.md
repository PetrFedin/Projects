---
phase: 02
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - _ai-share/synth-1-full/src/lib/b2b/replenishment-service.ts
  - _ai-share/synth-1-full/src/app/api/b2b/replenishment/suggest/route.ts
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-supply-panel.tsx
autonomous: true
requirements:
  - "AI Auto-Replenishment"
must_haves:
  truths:
    - User can request material purchase suggestions
    - Suggestions account for BOM quantity, PO plan, and wastage
  artifacts:
    - path: _ai-share/synth-1-full/src/lib/b2b/replenishment-service.ts
      provides: Replenishment calculations
    - path: _ai-share/synth-1-full/src/app/api/b2b/replenishment/suggest/route.ts
      provides: Suggestion API
  key_links:
    - from: _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-supply-panel.tsx
      to: /api/b2b/replenishment/suggest
      via: fetch
---

<objective>
Implement AI Auto-Replenishment logic to calculate purchase suggestions based on PO and BOM.

Purpose: Prevent stockouts and optimize raw material inventory by accurately forecasting needs with wastage.
Output: Replenishment calculation engine, endpoint, and UI suggestion block.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/2/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Replenishment Service and API</name>
  <files>
    - _ai-share/synth-1-full/src/lib/b2b/replenishment-service.ts
    - _ai-share/synth-1-full/src/app/api/b2b/replenishment/suggest/route.ts
  </files>
  <action>Create a service that takes a BOM array and a planned production quantity (e.g. 500 units), calculates raw requirements, applies a 5-10% wastage buffer, and returns a suggestion array. Wire this to the POST endpoint utilizing `next/server` standard routing.</action>
  <verify>POST request returns an array of suggestions with wastage applied</verify>
  <done>Service math is correct and API is fully functional</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Suggestions in Supply Panel</name>
  <files>
    - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-supply-panel.tsx
  </files>
  <action>Add an "AI Авто-пополнение" button inside the `workshop2-article-workspace-supply-panel.tsx`. When clicked, call the suggestion API using a default PO quantity (e.g. 100). Use the suggested values to fill missing quantity fields automatically via `mergeBundle`.</action>
  <verify>Clicking the AI suggest button correctly fetches and surfaces calculated quantities</verify>
  <done>UI correctly handles replenishment trigger and updates BOM state</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Suggestion request payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Suggest API | mitigate | Validate inputs (quantities >= 0) via Zod schemas to prevent negative purchase logic |
</threat_model>
