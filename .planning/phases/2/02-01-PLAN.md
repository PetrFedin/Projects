---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _ai-share/synth-1-full/src/lib/b2b/vendor-connect.ts
  - _ai-share/synth-1-full/src/app/api/b2b/vendor/item/[vendorItemId]/route.ts
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-supply-panel.tsx
autonomous: true
requirements:
  - "B2B Vendor Connect"
must_haves:
  truths:
    - User can link a BOM line to a vendor catalog item
    - User sees live stock and lead time for linked items
  artifacts:
    - path: _ai-share/synth-1-full/src/app/api/b2b/vendor/item/[vendorItemId]/route.ts
      provides: Live vendor data mock
    - path: _ai-share/synth-1-full/src/lib/b2b/vendor-connect.ts
      provides: API integrations logic
  key_links:
    - from: _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-supply-panel.tsx
      to: /api/b2b/vendor/item/[vendorItemId]
      via: fetch using React Query
---

<objective>
Implement live B2B Vendor Connect for BOM lines.

Purpose: Allow buyers to see real-time stock and lead time without leaving the article workspace.
Output: BFF endpoint, mock vendor integration, and React Query polling in the supply panel.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/2/02-RESEARCH.md

<interfaces>
From src/lib/production/workshop2-dossier-phase1.types.ts:
```typescript
export interface Workshop2BomLine {
  id: string;
  label: string;
  qty?: number;
  unit?: string;
  costPerUnit?: number;
  sourceNote?: string;
  leadTimeDays?: number;
  vendorItemId?: string; // Will be added
  status: 'draft' | 'ordered' | 'in_transit' | 'at_factory' | 'reserved' | 'consumed';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BFF endpoint and mock adapter</name>
  <files>
    - _ai-share/synth-1-full/src/lib/b2b/vendor-connect.ts
    - _ai-share/synth-1-full/src/app/api/b2b/vendor/item/[vendorItemId]/route.ts
  </files>
  <action>Create a unified vendor mock adapter in `vendor-connect.ts` returning static stock and lead times based on the ID. Create a Next.js GET route that returns this data securely.</action>
  <verify>curl or fetch to `/api/b2b/vendor/item/123` returns JSON with stock and lead time</verify>
  <done>Endpoint exists and returns 200 with valid DTO</done>
</task>

<task type="auto">
  <name>Task 2: Integrate React Query in Supply Panel</name>
  <files>
    - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-supply-panel.tsx
  </files>
  <action>Add a `useQuery` hook for live stock polling per line that has a `vendorItemId`. Render a badge with live stock status (e.g. "В наличии: 500" or "Нет в наличии") next to the item name. Implement linking by adding a dummy `vendorItemId` when clicking "RFQ / Бронь". Note: Use Lucide icons and keep consistent with existing Tailwind styles.</action>
  <verify>UI renders stock badges for linked items without crashing.</verify>
  <done>Live polling active via React Query, data displayed accurately</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> BFF | Next.js API boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | BFF Endpoint | mitigate | Require valid user session before returning vendor data |
| T-02-02 | Elevation of Privilege | BFF Endpoint | mitigate | Verify user has access to the vendor data via organization scope |
</threat_model>
