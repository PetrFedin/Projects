---
phase: 03-sample-and-fit
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - _ai-share/synth-1-full/package.json
  - _ai-share/synth-1-full/src/components/brand/production/fit-3d-viewer.tsx
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-fit-gold-panel.tsx
autonomous: true
requirements: ["3D Fit & Avatar Overlay"]
must_haves:
  truths:
    - "User can view a 3D model of the garment in the browser"
    - "User can see tension maps overlaying the 3D model"
  artifacts:
    - path: "_ai-share/synth-1-full/src/components/brand/production/fit-3d-viewer.tsx"
      provides: "React Three Fiber viewer component"
  key_links:
    - from: "workshop2-article-workspace-fit-gold-panel.tsx"
      to: "fit-3d-viewer.tsx"
      via: "component rendering with GLB URL"
---

<objective>
Интеграция 3D-вьювера для GLB/OBJ с картой натяжения (Tension Map).

Purpose: Визуальная оценка натяжения ткани и посадки (Heatmap) в браузере без необходимости открывать тяжелые CAD-программы.
Output: Настроенный 3D-движок R3F и компонент Fit3DViewer.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/3/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup 3D Dependencies</name>
  <files>_ai-share/synth-1-full/package.json</files>
  <action>
    Добавить `three`, `@react-three/fiber`, `@react-three/drei`, `dxf-parser` в зависимости проекта. Добавить `@types/three` в devDependencies (если не установлено).
  </action>
  <verify>
    <automated>npm install --prefix _ai-share/synth-1-full</automated>
  </verify>
  <done>Зависимости добавлены и установлены.</done>
</task>

<task type="auto">
  <name>Task 2: Create 3D Viewer Component</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/fit-3d-viewer.tsx</files>
  <action>
    Создать компонент `Fit3DViewer` на основе `@react-three/fiber` и `@react-three/drei`.
    Использовать `useGLTF` с включенной Draco-компрессией для загрузки модели.
    Добавить поддержку наложения `tensionMapUrl` через `useTexture` (наложение текстуры в `emissiveMap` материала с Traverse сцены).
    Обеспечить fallback (Loader) с использованием Suspense.
  </action>
  <verify>
    <automated>npm run lint --prefix _ai-share/synth-1-full -- _ai-share/synth-1-full/src/components/brand/production/fit-3d-viewer.tsx</automated>
  </verify>
  <done>Компонент 3D вьювера создан, поддерживает Draco и Tension Maps.</done>
</task>

<task type="auto">
  <name>Task 3: Embed 3D Viewer in Fit Gold Panel</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-fit-gold-panel.tsx</files>
  <action>
    Интегрировать `Fit3DViewer` в карточку сессии примерки (`FitSessionCard`) или в общее пространство панели, если присутствует 3D-модель (добавить кнопку "Посмотреть 3D" открывающую диалог/область с рендером).
  </action>
  <verify>
    <automated>npm run typecheck --prefix _ai-share/synth-1-full</automated>
  </verify>
  <done>Кнопка и просмотрщик интегрированы без нарушения верстки.</done>
</task>

</tasks>

<verification>
Убедиться, что 3D-вьювер не блокирует основной поток, использует Suspense и корректно компилируется TypeScript.
</verification>

<success_criteria>
Успешный рендеринг тестовой GLB модели с Draco-компрессией и возможность просмотра Heatmap.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-02-SUMMARY.md`
</output>
