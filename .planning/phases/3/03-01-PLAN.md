---
phase: 03-sample-and-fit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _ai-share/synth-1-full/src/lib/production/article-workspace/types.ts
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-fit-gold-panel.tsx
autonomous: true
requirements: ["CAD Version Control"]
must_haves:
  truths:
    - "User can link a specific CAD version (DXF/PLT) to a fit session"
    - "CAD version changes are immutable and append-only"
  artifacts:
    - path: "_ai-share/synth-1-full/src/lib/production/article-workspace/types.ts"
      provides: "CadVersion type and FitSession updates"
    - path: "_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-fit-gold-panel.tsx"
      provides: "UI for CAD version selection"
  key_links:
    - from: "FitSession"
      to: "CadVersion"
      via: "cadVersionId foreign key"
---

<objective>
Реализация жесткой связки версий лекал (DXF/PLT) с итерациями сэмпла.

Purpose: Предотвращение рассинхрона между внесенными дельтами на примерке и актуальными лекалами в конструкторском отделе. 
Output: Обновленные типы и интерфейс панели Fit Session для работы с CAD-версиями.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/3/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend FitSession types for CAD Versioning</name>
  <files>_ai-share/synth-1-full/src/lib/production/article-workspace/types.ts</files>
  <action>
    Добавить интерфейс `CadVersion` (поля id, articleId, versionNumber, fileUrl, createdAt).
    Обновить `FitSession` добавив поле `cadVersionId: string | null` и `aiFitAnalysis` (для следующего этапа) с полями `wrinklesDetected: string[]` и `recommendations: string[]`.
  </action>
  <verify>
    <automated>npm run typecheck --prefix _ai-share/synth-1-full</automated>
  </verify>
  <done>Интерфейсы добавлены, проект проходит проверку типов.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate CAD Versioning into Fit Gold Panel</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-fit-gold-panel.tsx</files>
  <action>
    В компоненте `FitSessionCard` отобразить идентификатор привязанной CAD-версии (`session.cadVersionId`). При создании новой сессии в `Workshop2ArticleFitGoldPanel` добавить заглушку для выбора или установки `cadVersionId`. Логика должна подразумевать append-only работу с лекалами.
  </action>
  <verify>
    <automated>npm run lint --prefix _ai-share/synth-1-full -- _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-fit-gold-panel.tsx</automated>
  </verify>
  <done>Компонент успешно рендерит CAD версию без ошибок линтера.</done>
</task>

</tasks>

<verification>
Убедиться, что типы корректно используются в компонентах, и UI позволяет видеть связь примерки и версии лекал.
</verification>

<success_criteria>
Типы обновлены, TypeScript не выдает ошибок. В интерфейсе Fit Gold Panel отображается информация о CAD-версии.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-01-SUMMARY.md`
</output>
