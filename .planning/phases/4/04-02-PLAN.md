---
phase: 04
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - _ai-share/synth-1-full/src/app/api/brand/workshop2/ai/capacity/route.ts
  - _ai-share/synth-1-full/src/components/brand/production/Workshop2PredictiveRiskPanel.tsx
autonomous: true
requirements: [2]

must_haves:
  truths:
    - User can predict capacity risks based on Tech Pack complexity
    - Risk panel fetches data and displays AI-driven capacity evaluation and rationale
  artifacts:
    - path: _ai-share/synth-1-full/src/app/api/brand/workshop2/ai/capacity/route.ts
      provides: AI endpoint for capacity calculation
  key_links:
    - from: _ai-share/synth-1-full/src/components/brand/production/Workshop2PredictiveRiskPanel.tsx
      to: _ai-share/synth-1-full/src/app/api/brand/workshop2/ai/capacity/route.ts
      via: fetch AI capacity prediction on user action
---

<objective>
Создание AI Capacity Planner для предсказания рисков и оценки нагрузки.

Purpose: Автоматическое предсказание возможных задержек на основе сложности ТЗ и текущей нагрузки экспериментального цеха.
Output: Работающий AI-endpoint и интегрированная UI панель для запроса и отображения данных.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/4/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AI Capacity Prediction API</name>
  <files>_ai-share/synth-1-full/src/app/api/brand/workshop2/ai/capacity/route.ts</files>
  <action>Create a POST endpoint returning JSON `{ predictedDays: number, riskLevel: string, rationale: string }`. Use `withTokenAudit` wrapper and Gemini 1.5 Flash via `genkit`. The prompt should accept a global Sample Room Load (e.g., High/Medium/Low) and Tech Pack details (e.g., complexity). Use `generateObject` with Zod schema to ensure structured output. Enforce B2B role via standard API guards (`withB2BV1ApiActorRoleHeaders` or local equivalent) if applicable.</action>
  <verify>
    <automated>npm run typecheck --prefix _ai-share/synth-1-full</automated>
  </verify>
  <done>Endpoint `POST /api/brand/workshop2/ai/capacity` exists, validates Zod body, and securely queries genkit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AI to Risk Panel</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/Workshop2PredictiveRiskPanel.tsx</files>
  <action>Add a button ("Оценить риски производства сэмпла" or similar) and state for fetching results from `/api/brand/workshop2/ai/capacity`. Formulate body payload using `dossier` details and a mock room load. Display the returned `riskLevel`, `predictedDays`, and `rationale` in a visually distinct, enterprise-grade panel area (use `Alert`, `LucideIcons`, and `text-sm` typography conventions).</action>
  <verify>
    <automated>npm run typecheck --prefix _ai-share/synth-1-full</automated>
  </verify>
  <done>Risk Panel triggers the API call, handles loading state, and renders the AI capacity prediction results visually.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | AI endpoint takes in arbitrary tech pack notes to evaluate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | /api/.../ai/capacity | mitigate | Sanitize JSON inputs and enforce strict Zod schemas for Gemini `generateObject` structure to prevent prompt injections altering return formats. |
</threat_model>

<verification>
Run `npm run typecheck --prefix _ai-share/synth-1-full`. Manually inspect API behavior via mock calls if possible to ensure GenKit responds correctly.
</verification>

<success_criteria>
The AI capacity predictor seamlessly evaluates the dossier context and gives a structured risk response.
</success_criteria>

<output>
After completion, create `.planning/phases/4/04-02-SUMMARY.md`
</output>
