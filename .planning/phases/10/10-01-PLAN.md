---
phase: 10-change-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _ai-share/synth-1-full/src/lib/production/workshop2-dossier-phase1.types.ts
  - _ai-share/synth-1-full/src/components/brand/production/Workshop2Phase1DossierPanel.tsx
  - _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-passport-panel.tsx
autonomous: true
requirements: ["Откат стадий", "Change Request (CR)"]
must_haves:
  truths:
    - User can log a Change Request during the production phase.
    - User can rollback the lifecycle state of the dossier to "Development" if a sample is rejected.
  artifacts:
    - path: _ai-share/synth-1-full/src/components/brand/production/Workshop2Phase1DossierPanel.tsx
      provides: Rollback button in the lifecycle state UI.
---

<objective>
Добавить механизм Change Request и отката стадий (Rollback) без потери историчности процесса разработки.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/WORKSHOP_2_ENTERPRISE_SCENARIOS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Типизация CR и истории</name>
  <files>_ai-share/synth-1-full/src/lib/production/workshop2-dossier-phase1.types.ts</files>
  <action>
    Добавить массив `changeRequests` (id, description, status: 'pending'|'approved'|'rejected', requestedBy, createdAt) в `Workshop2DossierPhase1`.
  </action>
  <verify>
    <automated>npm run typecheck:w2 --prefix _ai-share/synth-1-full</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: UI для запроса изменений (CR)</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-passport-panel.tsx</files>
  <action>
    Внизу паспорта (или в блоке статуса) добавить секцию "Запросы на изменение (CR)". Выводить список существующих CR и кнопку "Создать CR", которая добавляет мок-запись со статусом pending.
  </action>
  <verify>
    <automated>npm run lint --prefix _ai-share/synth-1-full -- _ai-share/synth-1-full/src/components/brand/production/workshop2-article-workspace-passport-panel.tsx</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Кнопка Rollback</name>
  <files>_ai-share/synth-1-full/src/components/brand/production/Workshop2Phase1DossierPanel.tsx</files>
  <action>
    В хедере, где отображается `lifecycleState`, добавить кнопку "Откатить в разработку" (появляется только если статус 'in_production' или 'handoff_ready'). Эта кнопка меняет статус обратно на 'draft' или 'development', сбрасывая готовность ТЗ, но сохраняя все данные.
  </action>
  <verify>
    <automated>npm run typecheck:w2 --prefix _ai-share/synth-1-full</automated>
  </verify>
</task>

</tasks>

<success_criteria>
В UI можно создать Change Request и нажать кнопку отката стадии.
</success_criteria>

<output>
After completion, create `.planning/phases/10/10-01-SUMMARY.md`
</output>