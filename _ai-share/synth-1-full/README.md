# Synth-1 — Web (Next.js)

UI для Synth-1 Fashion OS. Правила для агентов и дизайн: **`AGENTS.md`**.

**Монорепо:** API в корне репозитория; этот каталог — фронт. **`../../docs/RUNBOOK.md`** — два корня, CI, env, где demo/mock.

```bash
cd _ai-share/synth-1-full && npm ci && npm run dev
```
