# Архив: `Workshop2FloorToolkitBridge` («Инструменты пола»)

**Дата снятия с экрана:** 2026-04-22

## Что это было

Плавающий мост к инструментам пола (ТЗ, образец) в карточке артикула `workshop2` — `src/components/brand/production/Workshop2FloorToolkitBridge.tsx`. Подключался на странице артикула `c/[collectionId]/a/[articleId]/page.tsx`.

## Почему убрали

Создавал визуальный шум в минималистичном контуре «Разработка»; сценарии пола остаются доступны из основного ТЗ и маршрута без отдельного оверлея.

## Код

- Компонент-файл **не удалён** из репозитория, чтобы не ломать импорты, если появятся внутренние ссылки. На странице артикула **больше не рендерится**.
