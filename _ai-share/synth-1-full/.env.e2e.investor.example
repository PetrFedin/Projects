# Investor E2E / demo (Wave 58) — скопируйте в .env.local для постоянного demo-ready:
#   cp .env.e2e.investor.example .env.local
#
# Важно: Next.js читает env при старте процесса. После export в shell
# перезапустите dev: npm run dev:e2e:investor  (не plain dev:e2e).

WORKSHOP2_INVESTOR_DEMO_MODE=true
WORKSHOP2_UNIT_TESTS_PASSING=true
NEXT_PUBLIC_WORKSHOP2_INVESTOR_DEMO_MODE=true

E2E=true
NEXT_PUBLIC_E2E=true
NEXT_PUBLIC_DISABLE_FONTS=1
SYNTH_SKIP_ENTERPRISE_BOOTSTRAP=1
NEXT_DIST_DIR=.next-e2e

# Опционально — порт E2E (default 3123)
# PLAYWRIGHT_E2E_PORT=3123

# Опционально — отключить polling /api/notifications/feed на hub (investor demo perf)
NEXT_PUBLIC_WORKSHOP2_DISABLE_NOTIFICATIONS_POLL=true

# Curl timeouts для investor-demo-full (сек)
# WORKSHOP2_INVESTOR_CURL_MAX_SEC=60
# WORKSHOP2_INVESTOR_CURL_RETRIES=4
