/**
 * Реэкспорт для сплит-панелей (дашборды, превью). См. https://github.com/bvaughn/react-resizable-panels
 * v4: `Group` / `Separator` — алиасы под старые имена shadcn/legacy.
 */
export { Group as PanelGroup, Panel, Separator as PanelResizeHandle } from 'react-resizable-panels';
export type { PanelImperativeHandle as ImperativePanelHandle } from 'react-resizable-panels';
